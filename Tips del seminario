git reflog
en la carpeta .git estan todos los commits que alguna vez existieron

es para recuperar archivos se se borro todo el repo
-------

archivos de configuracion para poder importar en eclipse
mvn eclipse:eclipse

-------
git ignore .io

para las cosas que normalmente aparecen en ciertas cosas, y saber qué cosas hay que ignorar

---
el nombre del archivo gitignore es ".gitignore" 
es un archivo oculto

---
carpetas creadas:
source -> main -> java
source -> test > java

es para convenciones de maven - que separa las cosas por lenguaje

------
Refacto -> rename
para cuando a un metodo le quiero cambiar el nombre en todos los lugares donde se lo utiliza